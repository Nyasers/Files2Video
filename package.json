{
  "name": "f2v",
  "version": "1.0.0",
  "description": "AVI 直写直读，文件隐写不压缩",
  "private": true,
  "scripts": {
    "build": "rspack build --config rspack.config.cjs",
    "dev": "set NODE_ENV=development && rspack dev --config rspack.config.cjs"
  },
  "devDependencies": {
    "@rspack/cli": "^2.1.1",
    "@rspack/core": "^2.1.1",
    "@rspack/dev-server": "^2.1.0",
    "css-loader": "^7.1.4",
    "html-minifier-terser": "^7.2.0"
  },
  "license": "MIT"
}